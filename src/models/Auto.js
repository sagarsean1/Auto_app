export default class Auto {
  constructor(
    id,
    auto_num,
    driver_name,
    driver_phone,
    driver_img,
    driver_proofs
  ) {
    this.id = id;
    this.auto_num = auto_num;
    this.driver_name = driver_name;
    this.driver_phone = driver_phone;
    this.driver_img = driver_img;
    this.driver_proofs = driver_proofs;
  }
}
